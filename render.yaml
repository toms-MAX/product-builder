services:
  - type: web
    name: qmaker-backend
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --chdir . backend.app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
